project_name: how

builds:
  - id: how
    main: ./cmd/how/main.go
    binary: how
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: tar-archive
    format: tar.gz
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
  - id: zip-archive
    format: zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'

checksum:
  name_template: 'checksums.txt'

release:
  github:
    owner: Codilas
    name: how

changelog:
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
