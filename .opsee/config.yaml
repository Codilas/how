project:
  name: how
  type: web-app
  description: Project analysis failed - using fallback configuration
codebase_map:
  note: Analysis failed - manual review required
tech_stack:
  detected: false
  note: Could not analyze tech stack automatically
purpose:
  note: Project purpose could not be determined automatically
development:
  setup: npm install
  note: Default setup command - verify manually
verification:
  build: npm run build
  note: Default build command - verify manually
conventions:
  note: Code conventions could not be analyzed automatically
